/* import 'dart:convert';
import 'package:dio/dio.dart';
import 'package:kurs_cash/service/curs_data_model.dart';

/* class CursDataProvider {
  Future<List<CursDataModel>> getData() async {
    Response response =
        await Dio().get('https://www.cbr-xml-daily.ru/daily_json.js'); */

  /*   Map<String, dynamic> datadecode = jsonDecode(response.data);
    Map<String, dynamic> datadecode2 = datadecode['Valute'];
    print(datadecode2);
    return datadecode2.keys.toList().map((e) => CursDataModel.fromJson(e)).toList(); */

    /* List<dynamic> rateList = [];
    rateList = datadecode2.keys.toList().map((key) {
            return CursDataModel(
                iD: 1,
                charCode: key,
                value: datadecode2[key]['Previous'] / datadecode2[key]['Nominal'],
                name: datadecode2[key]['Name'],
                previous: datadecode2[key]['Value'] / datadecode2[key]['Nominal']);
          }).toList(); */
/*   } */
}
 */